import { Routes } from '@angular/router';
import {HelloCallComponent} from './components/hello-call/hello-call.component'

export const routes: Routes = [
    { path: 'hello', component: HelloCallComponent }
];
